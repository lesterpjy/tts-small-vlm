# Qwen2.5-VL-7B DTR N=4, M=4 on dev-200. Majority vote verification.
# Full budget: 16 candidates total.

variant: q25_dtr_n4m4
pipeline: msa
runner: dtr
tags: [dtr, N4_M4, majority_vote]

model:
  name: "Qwen/Qwen2.5-VL-7B-Instruct"
  backend: vllm
  dtype: bfloat16
  max_model_len: 16384

describe:
  n_samples: 4
  temperature: 0.7
  max_tokens: 1024

reason:
  m_samples: 4
  temperature: 0.7
  max_tokens: 1024
  use_cot: true

verify:
  method: majority_vote

evaluation:
  subset_size: 200
  seed: 42
  stratify: true
  min_per_language: 10
