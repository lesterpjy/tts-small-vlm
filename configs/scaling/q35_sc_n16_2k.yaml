# Qwen3.5-4B SC-N=16 on full validation (n=4651), max_new_tokens=2048.
# Doubled chain count for scaling comparison.

variant: q35_sc_n16_2k
pipeline: baseline
runner: self_consistency
tags: [baseline, self_consistency, N16, mmmu_prompt, val_full, max2k]

model:
  name: "Qwen/Qwen3.5-4B"
  backend: vllm
  dtype: bfloat16
  max_model_len: 16384

generation:
  prompt_style: mmmu
  temperature: 0.7
  max_new_tokens: 2048

self_consistency:
  n: 16

evaluation:
  dataset: examsv_validation
  subset_size: null
  seed: 42
