# Qwen2.5-VL-7B CoT baseline on full validation (n=4651).
# MMMU-style prompt (Yue et al. 2024) with free-text reasoning.

variant: q25_cot
pipeline: baseline
runner: cot
tags: [baseline, cot, mmmu_prompt, val_full]

model:
  name: "Qwen/Qwen2.5-VL-7B-Instruct"
  backend: vllm
  dtype: bfloat16
  max_model_len: 16384

generation:
  prompt_style: mmmu
  temperature: 0.0
  max_new_tokens: 1024

evaluation:
  subset_size: null
  seed: 42
